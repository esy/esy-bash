let name = "windows-default-manifest";
let version = "6.4.0-beta.2";
let tag = `v${version}`;
let tarballURL = `https://github.com/ManasJayanth/${name}/archive/refs/tags/${tag}.tar.gz`;
let tarballName = `${name}-${version}.tar.gz`;

module.exports = { name, version, tag, version, tarballURL, tarballName };
